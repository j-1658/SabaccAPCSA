import java.awt.*;
import javax.swing.JFrame;

public class DisplayGraphics extends Canvas {

    public void paint(Graphics g){
        //initial scene
        setBackground(Color.BLACK);
        g.setColor(Color.getHSBColor(Color.RGBtoHSB()))
    }
}
